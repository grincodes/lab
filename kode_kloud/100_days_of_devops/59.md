# Quests
Last week, the Nautilus DevOps team deployed a redis app on Kubernetes cluster, which was working fine so far. This morning one of the team members was making some changes in this existing setup, but he made some mistakes and the app went down. We need to fix this as soon as possible. Please take a look.

The deployment name is redis-deployment. The pods are not in running state right now, so please look into the issue and fix the same.

Note: The kubectl utility on jump_host has been configured to work with the kubernetes cluster.

# Process and Solutions

- k describe po <pod-name>
```
 Type     Reason       Age                  From               Message
  ----     ------       ----                 ----               -------
  Normal   Scheduled    11m                  default-scheduler  Successfully assigned default/redis-deployment-54cdf4f76d-kshl7 to kodekloud-control-plane
  Warning  FailedMount  93s (x13 over 11m)   kubelet            MountVolume.SetUp failed for volume "config" : configmap "redis-conig" not found
  Warning  FailedMount  39s (x5 over 9m46s)  kubelet            Unable to attach or mount volumes: unmounted volumes=[config], unattached volumes=[], failed to process volumes=[]: timed out waiting for the condition
```

- k get configmap
```
NAME               DATA   AGE
kube-root-ca.crt   1      34m
redis-config       2      14m
```

- k edit deployment redis-deployment
```
change volume name from redis-conig to redis-config 
and also change image from redis:alpne to  redis:alpine
```

- k get deploy to confirm deployment is running
