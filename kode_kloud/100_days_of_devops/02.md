# Quest
Create a temporal user (riva) with expiry date 2024-12-2 on server 2 .

# Process and Solution
- ssh into server 2 from jumphost using provided credentials
  ```ssh username@host```
- Enter this linux command on server to create user with no interactive shell
    ``` sudo useradd riva -e 2024-12-2```

