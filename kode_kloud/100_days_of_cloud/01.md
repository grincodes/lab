# Quest
Create Aws keypair and name it devops-kp.
The key pair type must be rsa.

# Process and Solution
- use this aws cli command
  ```aws ec2 create-key-pair --key-name devops-kp --key-type rsa --output text > devops-kp.pem```

